rootProject.name = 'SampleProject'

buildCache {
    local(DirectoryBuildCache) {
        enabled = true
        directory = new File(rootDir, 'build/build-cache')
        removeUnusedEntriesAfterDays = 10
    }
}
